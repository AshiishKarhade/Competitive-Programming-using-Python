#include <iostream>
using namespace std;

void add_one(int arr[]){
    
}

int main(){

    int n;
    cin >> n;
    int arr[n];
    for (int i=0; i<n;i++){
        cin >> arr[i];
    }

    add_one(arr);

    return 0;
}